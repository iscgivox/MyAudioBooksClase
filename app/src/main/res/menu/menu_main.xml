<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="net.ivanvega.myaudiobooksclase.MainActivity">

    <item android:id="@+id/menu_preferencias"
        android:icon="@android:drawable/ic_menu_preferences"
        android:orderInCategory="95"
        app:showAsAction="ifRoom"
        android:title="Preferencias"/>

    <item android:id="@+id/menu_ultimo"
    android:icon="@android:drawable/ic_menu_set_as"
    android:orderInCategory="100"
    app:showAsAction="ifRoom|withText"
    android:title="Ir al último"

        />


    <item android:id="@+id/menu_buscar"
        android:actionViewClass="android.widget.SearchView"
        android:icon="@android:drawable/ic_menu_search"
        android:orderInCategory="90"
        app:showAsAction="ifRoom|collapseActionView"
        android:title="Buscar"

        />

    <item android:id="@+id/menu_acerca"
        android:orderInCategory="110"
        app:showAsAction="never"
        android:title="Acerca de..."

        />

</menu>
